{% extends ':Front:base.html.twig' %}
{% block title %}
    PanameWorldTV - Ajout d'une vidéo
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container-fluid" id="background">
        <div class="row">
            <div class="col-lg-offset-3 col-lg-6 text-center">
                {{ form_start(
                form_create,
                {   'method': 'post',
                    'action': path('video_add'),
                    'attr': {
                    'id':'form_create',
                    'autocomplete':'off'}
                }
                ) }}
                <div class="col-lg-offset-4 col-lg-4">
                    {{ form_label(form_create.titre,"Titre") }}
                    {{ form_widget(form_create.titre, {'attr':{'class':'form-control'}})}}

                    {{ form_label(form_create.description,"Description") }}
                    {{ form_widget(form_create.description, {'attr':{'class':'form-control'}})}}

                    {{ form_label(form_create.youtubeId,"Youtube ID") }}
                    {{ form_widget(form_create.youtubeId, {'attr':{'class':'form-control'}})}}
                    <br>
                    <div class="text-center">
                        {{ form_widget(form_create.save,{'attr':{'class':'btn btn-info '}})}}
                        <a class="btn btn-warning" href="{{ path('dashboard_videos') }}">Retour</a>
                    </div>
                    <br>

                </div>
                {{ form_end(form_create) }}
            </div>
        </div>
    </div>
{% endblock %}